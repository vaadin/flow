<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>com.vaadin</groupId>
        <artifactId>vaadin-test-karaf</artifactId>
        <version>1.2-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>karaf-run</artifactId>
    <name>Vaadin Karaf Test Runner</name>

    <dependencies>
        <!--Vaadin dependencies should not be in a classpath-->
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>flow-server</artifactId>
            <version>${project.version}</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>pre-integration-test</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <executable>mvn</executable>
                            <async>true</async>
                            <asyncDestroyOnShutdown>true</asyncDestroyOnShutdown>
                            <commandlineArgs>-f karaf-run-pom.xml karaf:run</commandlineArgs>
                        </configuration>
                    </execution>
                </executions>

            </plugin>
            <plugin>
                <groupId>org.apache.karaf.tooling</groupId>
                <artifactId>karaf-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>karaf-client-ctrl</id>
                        <phase>pre-integration-test</phase>
                        <goals>
                            <goal>client</goal>
                        </goals>
                        <configuration>
                            <delay>5</delay>
                            <attempts>100</attempts>
                            <port>8181</port>
                            <commands>
                                <!--Scheduled shutdown in 15 minutes-->
                                <command>shutdown -f +10</command>
                                <command>feature:install http</command>
                                <command>feature:install http-whiteboard</command>
                                <command>feature:install webconsole</command>
                                <command>bundle:install -s mvn:org.jsoup/jsoup/1.8.3</command>
                                <command>bundle:install -s mvn:com.google.code.gson/gson/2.3.1</command>
                                <command>bundle:install -s mvn:com.vaadin.external/gentyref/1.2.0.vaadin1</command>
                                <command>bundle:install -s mvn:com.helger/ph-commons/9.1.2</command>
                                <command>bundle:install -s mvn:com.helger/ph-css/6.1.1</command>
                                <command>bundle:install -s mvn:com.vaadin.external.gwt/gwt-elemental/2.8.2.vaadin2</command>
                                <command>bundle:install -s mvn:net.bytebuddy/byte-buddy/1.9.0</command>
                                <command>bundle:install -s mvn:commons-io/commons-io/2.5</command>
                                <command>bundle:install -s mvn:commons-fileupload/commons-fileupload/1.3.3</command>
                                <command>bundle:install -s mvn:org.jsoup/jsoup/1.10.3</command>
                                <command>bundle:install -s mvn:com.vaadin/flow-server/${project.version}</command>
                                <command>bundle:install -s mvn:com.vaadin/flow-client/${project.version}</command>
                                <command>bundle:install -s mvn:com.vaadin/flow-html-components/${project.version}</command>
                                <!--<command>bundle:install -s mvn:com.vaadin/flow-osgi-integration/${project.version}</command>-->
                                <command>
                                    <![CDATA[bundle:install -s file:./../vaadin-karaf-bundle1/target/vaadin-karaf-bundle1-${project.version}.jar]]></command>
                                <command>
                                    <![CDATA[bundle:install -s file:./../vaadin-karaf-bundle2/target/vaadin-karaf-bundle2-${project.version}.jar]]></command>
                            </commands>
                        </configuration>
                    </execution>
                    <execution>
                        <id>karaf-client-shutdown</id>
                        <phase>post-integration-test</phase>
                        <goals>
                            <goal>client</goal>
                        </goals>
                        <configuration>
                            <delay>5</delay>
                            <attempts>10</attempts>
                            <commands>
                                <command>system:shutdown -f</command>
                            </commands>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>2.19.1</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
