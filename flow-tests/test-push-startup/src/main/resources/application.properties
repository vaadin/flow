server.port=${server.port}

# The servlet is initialized on the first request instead of on startup, so
# that the test decides when service initialization starts. The push endpoint
# is registered during servlet context initialization either way, which leaves
# it reachable while the service is not ready yet.
vaadin.load-on-startup=false
