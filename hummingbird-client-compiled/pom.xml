<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>com.vaadin</groupId>
		<artifactId>hummingbird-parent</artifactId>
		<version>1.0-SNAPSHOT</version>
	</parent>
	<artifactId>hummingbird-client-compiled</artifactId>
	<name>Hummingbird Client Compiled</name>
	<packaging>jar</packaging>
	<properties>
		<gwt.module.output>${project.build.outputDirectory}/VAADIN/gwt</gwt.module.output>
	</properties>
	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>com.google.gwt</groupId>
				<artifactId>gwt</artifactId>
				<version>${gwt.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<repositories>
		<repository>
			<id>google-snapshots</id>
			<url>https://oss.sonatype.org/content/repositories/google-snapshots/</url>
			<snapshots>
				<enabled>true</enabled>
			</snapshots>
		</repository>
	</repositories>

	<dependencies>
		<!-- Project dependencies -->
		<dependency>
			<groupId>com.vaadin</groupId>
			<artifactId>hummingbird-client</artifactId>
			<version>1.0-SNAPSHOT</version>
		</dependency>
		<dependency>
			<groupId>com.vaadin</groupId>
			<artifactId>hummingbird-client</artifactId>
			<version>1.0-SNAPSHOT</version>
			<classifier>sources</classifier>
		</dependency>
	</dependencies>

	<build>

		<!-- Output classes directly into the webapp, so that IDEs and "mvn process-classes" 
			update them in DevMode -->
<!-- 		<outputDirectory>${project.build.directory}/${project.build.finalName}/WEB-INF/classes</outputDirectory> -->
		<plugins>

			<!-- GWT Maven Plugin -->
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>gwt-maven-plugin</artifactId>
				<version>2.7.0</version>
				<!-- Remove dependencies when gwt-maven-plugin 2.8.0-SNAPSHOT is available -->
				<dependencies>
					<dependency>
						<groupId>com.google.gwt</groupId>
						<artifactId>gwt-user</artifactId>
						<version>${gwt.version}</version>
					</dependency>
					<dependency>
						<groupId>com.google.gwt</groupId>
						<artifactId>gwt-dev</artifactId>
						<version>${gwt.version}</version>
					</dependency>
					<dependency>
						<groupId>com.google.gwt</groupId>
						<artifactId>gwt-codeserver</artifactId>
						<version>${gwt.version}</version>
					</dependency>
				</dependencies>

				<executions>
					<execution>
						<goals>
							<goal>compile</goal>
							<goal>test</goal>
							<goal>generateAsync</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<failOnError>true</failOnError>
					<!-- <logLevel>TRACE</logLevel> -->
					<!-- <runTarget>client.html</runTarget> -->
					<gwtSdkFirstInClasspath></gwtSdkFirstInClasspath>
					<jsInteropMode>JS</jsInteropMode>
					<persistentunitcache>false</persistentunitcache>
					<webappDirectory>${gwt.module.output}</webappDirectory>
					<compileSourcesArtifacts>
						<compileSourcesAtrifact>com.vaadin:hummingbird-shared</compileSourcesAtrifact>
						<compileSourcesAtrifact>com.vaadin:hummingbird-server</compileSourcesAtrifact>
					</compileSourcesArtifacts>
					<modules>
						<module>com.vaadin.ClientEngine</module>
					</modules>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
