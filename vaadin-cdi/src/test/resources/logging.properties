# Weld logs its bootstrap and shutdown through java.util.logging once per test
# class, which floods the build log. Keep warnings and errors only.
handlers = java.util.logging.ConsoleHandler
.level = INFO
java.util.logging.ConsoleHandler.level = ALL
org.jboss.weld.level = WARNING
# Same for the Hibernate Validator version banner
org.hibernate.level = WARNING
