package com.vaadin.client;

import com.google.gwt.core.shared.GWT;

public class GwtExampleTest extends ClientEngineTestBase {
    /*
     * This is just a dummy test showing how GWT tests should be structured.
     * This test can be removed once there are regular tests.
     */
    public void testSomething() {
        assertTrue(GWT.isScript());
    }
}
