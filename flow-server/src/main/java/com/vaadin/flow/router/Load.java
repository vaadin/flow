package com.vaadin.flow.router;

/**
 * Defines when to load dependencies.
 */
public enum Load {
    /** Load when the application starts. */
    ON_STARTUP,
    /** Load when needed. */
    ON_DEMAND
}
