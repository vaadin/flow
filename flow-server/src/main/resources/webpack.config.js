/**
 * This file has been autogenerated as it didn't exist or was made for an older incompatible version.
 * This file can be used for manual configuration. It will not be modified if the flowDefaults constant exists.
 */
const merge = require('webpack-merge');
const flowDefaults = require('./webpack.generated.js');

/**
 * Modify the default webpack config generated by Vaadin
 */
// e.g. update the rules to also transpile `.mjs` files
// if (!flowDefaults.module.rules[0].test) {
//   throw "Unexpected structure in generated webpack config";
// }
// flowDefaults.module.rules[0].test = /\.m?js$/;

/**
 * Or add new configuration in the merge block
 */
// e.g. add a custom webpack plugin
// (install this plugin with `npm install --save-dev webpack-bundle-analyzer`)
// const BundleAnalyzerPlugin = require('webpack-bundle-analyzer').BundleAnalyzerPlugin;
module.exports = merge(flowDefaults, {
  // plugins: [
  //   new BundleAnalyzerPlugin({analyzerMode: 'static'})
  // ]
});
